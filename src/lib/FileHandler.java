package lib;

/**
 * Should be in charge of creating a folder that contains
 * the specific result fetched from the startpage
 */
public class FileHandler {
    private String startPage;

    FileHandler(String startPage) {
        this.startPage = startPage;
    }
}
